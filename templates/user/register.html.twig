{% extends 'base.html.twig' %}

{% block title %}{{ title }}{% endblock %}

{% block content %}

    <div class="text-center mb-3">
        <h2>Vous n'avez pas de compte ?</h2>
        <h6 class="d-block mx-auto" style="font-size: 0.9rem; max-width: 360px;">Inscrivez-vous et bénéficiez des avantages d'avoir un compte client</h6>
    </div>
    <div class="mx-auto d-block" style="max-width: 400px;">
        {{ form_start(form) }}
            {{ form_label(form.firstname, 'Firstname', { 'label_attr' : { 'class' : 'col-sm-12 col-lg-12 fw-bold'}}) }}
            {{ form_widget(form.firstname, { 'attr' : { 'class' : 'mb-2', 'autofocus' : true}}) }}
            {{ form_label(form.lastname, 'Lastname', { 'label_attr' : { 'class' : 'col-sm-12 col-lg-12 fw-bold'}}) }}
            {{ form_widget(form.lastname, { 'attr' : { 'class' : 'mb-2'}}) }}
            {{ form_label(form.email, 'Email', { 'label_attr' : { 'class' : 'col-sm-12 col-lg-12 fw-bold'}}) }}
            {% if emailError|default %}
                <div class="invalid-feedback d-block mt-0">
                    <span class="badge bg-danger text-white mb-1 me-1">ERROR</span>{{ emailError }}
                </div>
            {% endif %}
            {{ form_widget(form.email, { 'attr' : { 'class' : 'mb-2'}}) }}
            {{ form_label(form.password.first, "Password", { 'label_attr' : { 'class' : 'col-sm-12 col-lg-12 fw-bold'}}) }}
            {{ form_errors(form.password.first) }}
            {{ form_widget(form.password.first, { 'attr' : {'class' : 'mb-2',}}) }}
            {{ form_label(form.password.second, "Confirm password", { 'label_attr' : { 'class' : 'col-sm-12 col-lg-12 fw-bold'}}) }}
            {{ form_widget(form.password.second, { 'attr' : {'class' : 'mb-2',}}) }}
            {{ include('security/_compromised_password.html.twig') }}
        <div class="d-flex justify-content-center">
            <button type="submit" class="btn btn-success py-2 px-5 fs-4 mt-3 mb-2">Créer mon compte</button>
        </div>
        <div class="d-flex justify-content-center">
            <p class="text-center mt-2" style="font-size: 0.8rem; max-width: 500px;">
                En cliquant sur « Créer mon compte », j'accepte le traitement de mes données personnelles et je déclare
                expressément accepter les CGV et l'Avis concernant la protection des données de La Boutique Française.
            </p>
        </div>
        {{ form_end(form) }}
    </div>

{% endblock %}
